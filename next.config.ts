const nextConfig = {
/*    eslint: {
        // ⚠️ Ignora errores de ESLint durante el build
        ignoreDuringBuilds: true,
    },
    typescript: {
        // ⚠️ Si también tienes errores de TypeScript
        ignoreBuildErrors: true,
    },*/
}

export default nextConfig;
